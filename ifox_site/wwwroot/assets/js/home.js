(function(){ 'use strict'; document.addEventListener('DOMContentLoaded', function(){ var globe=document.getElementById('ifox-globe'); if(!globe) return; var reduced=window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches; if(!('ontouchstart' in window) && !reduced){ window.addEventListener('mousemove',function(e){ var x=(e.clientX-window.innerWidth/2)/(window.innerWidth/2); var y=(e.clientY-window.innerHeight/2)/(window.innerHeight/2); globe.style.transform='translate3d('+(x*6)+'px,'+(y*4)+'px,0)'; }); } }); })();